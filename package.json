{
  "name": "my-es6-module",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "coffee -c -o dist src/**/*.coffee",
    "build:watch": "npm run build -- --watch",
    "test": "mocha --compilers coffee:coffee-script/register src/**/_spec/*.coffee",
    "test:watch": "npm run test -- --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "coffee-script": "^1.10.0",
    "mocha": "^2.3.4"
  }
}
