{
  "name": "my-es6-module",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel --ignore _spec -d dist src",
    "build:watch": "npm run build -- --watch",
    "test": "mocha --compilers js:babel-core/register src/**/_spec/*.js",
    "test:watch": "npm run test -- --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "mocha": "^2.3.4"
  }
}
